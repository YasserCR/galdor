// Package ui holds the embedded assets and HTTP handlers for galdor's
// observability dashboard. Everything served is built into the binary
// via embed.FS so the framework remains a single artefact.
//
// Status: stub (Phase 5).
package ui
