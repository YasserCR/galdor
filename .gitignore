# Binaries
*.exe
*.dll
*.so
*.dylib
/bin/
/dist/
/galdor
# The CLI binary built inside its own module dir (cmd/galdor is its own module).
/cmd/galdor/galdor
# Stray binaries produced by `go build .` inside an example dir (the binary
# is named after the directory, both at the repo root and inside the dir).
/integration-*
/eval-*
/memory-*
/graph-*
/tools-*
/observability-*
/provider-*
/scry-*
/structured-output
/trial-suite
/cast-agent
/council-team
/spellbook
examples/*/integration-*
examples/*/eval-suite
examples/*/memory-rag
examples/*/graph-*
examples/*/tools-loop
examples/*/observability-trace
examples/*/provider-interface
examples/*/scry-store
examples/*/agent-react
examples/*/structured-output

# Test
*.test
*.out
coverage.txt
coverage.html

# Go
vendor/
# go.work and go.work.sum ARE committed: this is a multi-module repo
# (providers/<name>/, memory/<backend>/) and the workspace is the
# canonical dev setup.

# IDE / OS
.idea/
.vscode/
.DS_Store
*.swp

# Local
.env
.env.local
*.local

# Private working notes (debug / audit plans, not for the repo)
/debug/

# Embedded storage artefacts
*.bolt
*.db
*.sqlite
*.sqlite-journal
/observability-trace
/scry-store
