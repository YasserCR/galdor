# Binaries
*.exe
*.dll
*.so
*.dylib
/bin/
/dist/
/galdor
# The CLI binary built inside its own module dir (cmd/galdor is its own module).
/cmd/galdor/galdor
# Stray binaries produced by `go build .` inside an example dir.
/integration-*
/eval-*
/memory-*
/graph-*
/tools-*
/observability-*
/provider-*
/scry-*

# Test
*.test
*.out
coverage.txt
coverage.html

# Go
vendor/
# go.work and go.work.sum ARE committed: this is a multi-module repo
# (providers/<name>/, memory/<backend>/) and the workspace is the
# canonical dev setup.

# IDE / OS
.idea/
.vscode/
.DS_Store
*.swp

# Local
.env
.env.local
*.local

# Private working notes (debug / audit plans, not for the repo)
/debug/

# Embedded storage artefacts
*.bolt
*.db
*.sqlite
*.sqlite-journal
/observability-trace
/scry-store
